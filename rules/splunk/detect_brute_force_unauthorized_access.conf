index=* sourcetype="docker:json"
| rex field=line "^(?<client_ip>\d+\.\d+\.\d+\.\d+)\s+-\s+-\s+\[[^\]]+\]\s+\"\w+\s+(?<uri>[^\"]+)\"\s+(?<status>\d+)"
| search status=403 OR status=401
| stats count by client_ip, uri, status
| where count > 10
| table _time, client_ip, uri, status
